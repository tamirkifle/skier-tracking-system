# Copy to .env and adjust. Every value below has a working default in
# application.properties, so an empty .env still runs against `make up`.

# --- Broker -----------------------------------------------------------------
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest

# --- Server tuning ----------------------------------------------------------
SERVER_PORT=8080
